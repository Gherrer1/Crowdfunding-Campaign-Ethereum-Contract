const routes = require('next-routes')();

// routes
// .add('campaigns', '/campaigns/:id')
// .add('requests', '/campaigns/:id/requests')
// .add('newRequest', '/campaigns/:id/requests/new');

module.exports = routes;